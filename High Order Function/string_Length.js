// Array String Length

// Given an array of strings print the length of each string

// Screenshot of the output for the array ["apple", "windows", "ubuntu"]

var arrays = ["apple", "windows", "ubuntu"];

arrays.forEach(function(num){
    console.log(num.length);
})
